# =============================================================================
# Environment Variables Template for Doctor Cornelius Backend
# =============================================================================
# Copy this file to .env and fill in your actual values:
#   cp .env.example .env
#
# IMPORTANT: Never commit your .env file to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# Slack Configuration
# -----------------------------------------------------------------------------
# Bot token for authenticating API calls to Slack
# Starts with 'xoxb-' - get this from your Slack App's OAuth & Permissions page
SLACK_BOT_TOKEN=xoxb-your-bot-token

# Signing secret for verifying incoming requests from Slack
# Found in your Slack App's Basic Information page under "App Credentials"
SLACK_SIGNING_SECRET=your-signing-secret

# App-level token for Socket Mode connections
# Starts with 'xapp-' - generate this in your Slack App's Basic Information page
# Required for Socket Mode which allows the bot to receive events without a public URL
SLACK_APP_TOKEN=xapp-your-app-token

# -----------------------------------------------------------------------------
# Neo4j Database Configuration
# -----------------------------------------------------------------------------
# Connection URI for Neo4j database
# Use 'bolt://localhost:7687' for local development
# Use 'neo4j+s://your-host:7687' for remote/cloud connections with encryption
NEO4J_URI=bolt://localhost:7687

# Username for Neo4j authentication
# Default is 'neo4j' for most installations
NEO4J_USER=neo4j

# Password for Neo4j authentication
# Set this to your Neo4j database password
NEO4J_PASSWORD=password

# -----------------------------------------------------------------------------
# Google Gemini AI Configuration
# -----------------------------------------------------------------------------
# API key for Google Gemini
# Get this from https://makersuite.google.com/app/apikey
GOOGLE_API_KEY=your-google-api-key

# Gemini model to use for AI responses
# Options: gemini-3-flash-preview, gemini-pro, gemini-pro-vision, etc.
GEMINI_MODEL=gemini-3-flash-preview

# -----------------------------------------------------------------------------
# Application Settings
# -----------------------------------------------------------------------------
# Enable debug mode for development (true/false)
# When true, provides more verbose logging and error messages
DEBUG=false

# Logging level for the application
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO
